# 🚀 AI 臨床復健平台 - 專案交接與研發計畫說明書

---

## 📌 專案概觀

| 項目 | 內容 |
|------|------|
| **專案名稱** | AI-Clinical-Rehab-Platform |
| **核心目標** | 利用 mHealth 感測器數據（加速度、重力分量、FFT 能量）實現即時復健動作辨識與臨床品質監控，並提供生物回饋評分 |
| **目前進度** | Stage 5 結案（產品化遷移與批量驗收完成） |
| **當前日期** | 2026-04-06 |

---

## 🛠️ 當前技術架構與檔案說明

### 核心檔案清單

| 檔案 | 說明 |
|------|------|
| `schema.py` | 核心邏輯庫。包含資料預處理（Low-pass Filter, FFT）、`ClinicalQualityGate`（品質攔截器）與 `RealTimeBiofeedbackEngine`（即時推論引擎） |
| `main.py` | 產品入口點。實現自動化批量測試與實時模擬，支持多受試者會期切換 |
| `models/clinical_rehab_model_v3.keras` | Stage 3 訓練之 v3.1_multi 模型。輸入維度為 $(1, 128, 8)$ |

---

### 數據規格 (Generation 3 - 8D Features)

每筆影格資料必須包含以下 **8 個特徵軸**：

| # | 特徵名稱 | 說明 |
|---|----------|------|
| 1 | `Acc_X` | 原始加速度 X 軸 |
| 2 | `Acc_Y` | 原始加速度 Y 軸 |
| 3 | `Acc_Z` | 原始加速度 Z 軸 |
| 4 | `Magnitude` | 合力向量 $\sqrt{x^2+y^2+z^2}$ |
| 5 | `Grav_X` | 低通濾波分離後的重力分量（必須執行全局縮放 ÷ 10.0） |
| 6 | `Grav_Y` | 低通濾波分離後的重力分量（必須執行全局縮放 ÷ 10.0） |
| 7 | `Grav_Z` | 低通濾波分離後的重力分量（必須執行全局縮放 ÷ 10.0） |
| 8 | `FFT_Energy` | 視窗內之頻譜能量，採 Log 縮放 $\frac{\log(1 + x)}{5.0}$ |

---

## ⚠️ 關鍵研發修正記錄 (Fatal Fixes)

> 接手 AI 必須注意以下兩點，這是確保系統不會崩潰或邏輯空轉的關鍵。

### Fix 1 — 維度對齊修復

- **問題**：曾發生 $(1, 128, 7)$ 維度錯誤
- **原因**：推論端遺漏了第 8 個特徵（FFT Energy）
- **修正時間**：Stage 5
- **現狀**：統一由 `schema.py` 的 `load_and_preprocess_subject` 提供標準 8D 數據

### Fix 2 — 重力縮放校準 (÷10.0)

- **問題**：這是最嚴重的 Bug
- **原因**：`schema.py` 遺漏了縮放，導致變異數放大 100 倍，使品質閘門完全失效
- **修正方式**：

  ```python
  gravity_values = gravity_values / 10.0
  ```

---

## 📊 臨床評分邏輯 (Scoring Logic)

系統採用**雙軌評分機制**，確保回饋的真實性：

### 品質分數 ($Score_{quality}$)

- 檢查 $Grav\_Y$ 變異數是否大於 `0.0005`
- 若低於此值，系統判定為靜態或無效動作，強制亮起 **🔴 紅燈 (RED / HALT)**

### 相似度分數 ($Score_{sim}$)

- 基於歐幾里德距離比對受試者與 **S10 黃金範本**

$$Score_{sim} = \max(0,\ 100 - Distance \times 15)$$

### 綜合評分 (Final Score)

$$Final = (Score_{quality} \times 0.4) + (Score_{sim} \times 0.6)$$

---

## 📅 未來研發規劃 (Roadmap)

### Stage 6 — 演算法魯棒性優化（DTW 實作）

**核心痛點**：目前 schema.py 中的動作相似度比對是採用歐幾里德距離（Euclidean Distance）。這是點對點的剛性計算，只要受試者在復健時的節奏或速度與黃金範本（S10）差了 0.05 秒，分數就會暴跌並觸發黃燈（🟡）。

**核心任務**：

- 重構相似度計算：在 schema.py 的 RealTimeBiofeedbackEngine 類別中，將原本的 calculate_similarity 演算法升級，正式引入 DTW（Dynamic Time Warping，動態時間規規整）。

- 時序動態對齊：利用 DTW 允許時間軸彈性伸縮（Time Warping）的特性，自動對齊受試者與黃金範本的物理訊號波峰。

- 多標籤模型適配性測試：除了目前的 Label 7（手臂前舉）之外，開始測試 v3 模型對其他復健動作標籤的即時推論與相似度比對。

**預期產出**：

- 更新後的 schema.py（具備 DTW 相似度計算能力）。

- 優化後的 綠燈（🟢） 穩定度：確保「姿勢完全正確、但節奏稍快或稍慢」的標準動作能被系統寬容接受，減少無效黃燈。

- 維持 ClinicalQualityGate 的嚴謹攔截（合格率應保持與 Week 5 一致，不影響防禦功能）。

### Stage 7 — 系統整合與 UI 橋接

**目前痛點**：目前所有的系統輸出（品質報告、AI預測、相似度分數）都只能在黑色的黑盒子控制台（Console/Terminal）中以文字印出，尚未與實際的產品前端畫面接軌。

**核心任務**：

- 建立數據橋接器（ui_bridge.py）：開發獨立的通信模組（例如透過 WebSocket 或網路 Socket 協定），將 main.py 核心引擎即時產出的分析數據向外串流。

- 前端視覺化對接：將引擎產出的 ui_color（紅、黃、綠狀態）與 final_score（綜合得分）即時傳送至前端 3D 視覺組件。

- 實時數據流壓力測試：模擬高頻率（50Hz 取樣率）的串流狀態，確保數據在品質閘門、AI 推論與前端顯示之間傳輸時，不會產生系統延遲（Latency）。

**預期產出**：

- 新增 ui_bridge.py 檔案。

- 實現「後端推論、前端即時亮燈」的完整系統整合（Integration Test），病患可在畫面上直接看到即時的綠、黃、紅燈生理回饋。

### Stage 8 — 最終壓力測試與交付

**目前痛點**：系統即將交付，必須確保在長時間、高併發或異常斷訊的臨床環境下，系統不會發生記憶體洩漏（Memory Leak）或無預警崩潰。

**核心任務**：

- 極限壓力測試（Stress Testing）：長時間循環運行全量受試者數據（S1–S10），測試 RealTimeStreamProcessor 的緩衝區在極端情況下的穩定性。

- 例外與錯誤處理（Exception Handling）：針對感測器斷訊、髒數據、硬體噪音或未定義動作標籤等臨床意外，建立完善的系統防護與自動重啟機制。

- 撰寫技術文檔與手冊：將 Week 1 到 Week 8 的研發歷程、8D 特徵技術規格、物理門檻參數（0.0005 變異數限制）以及系統啟動指令，完整整理為正式的部署指南（README.md 與技術白皮書）。

**預期產出**：

- 經過優化與封裝、可隨時部署上線的生產級代碼。

- 全套專案交付文件（包含系統架構圖、UAT 測試報告與環境部署手冊）。

- 專案正式結案。

---

## 📋 驗收數據參考 (Stage 5 Batch Report)

執行 `python main.py` 的預期表現：

| 受試者群組 | 品質合格率 | 說明 |
|-----------|-----------|------|
| S01 - S04（低活動組） | 3.5% - 16.5% | 大量攔截為**正確行為** |
| S10（黃金標準） | 約 20.6% | 僅在動態執行區間通過，靜態區間自動攔截 |

---

## 📝 指令給下一位 AI

請根據上述 Roadmap 執行 **Stage 6** 的任務。

**首要任務**：在 `schema.py` 中重構相似度計算，將**歐幾里德距離**替換為 **DTW 演算法**，並確保不破壞現有的 `ClinicalQualityGate` 攔截邏輯。
