[pytest]
testpaths = tests
markers =
    slow: 需要 TensorFlow 與已訓練模型的端到端測試（可用 -m "not slow" 略過）
    acceptance: 執行 main.py 並與 golden 基準逐行比對（耗時約 1 分鐘）
    perf: 效能煙霧測試，門檻寬鬆但足以攔截數量級退化
    convention: 規範、文件一致性與 notebook 相容性等靜態檢查（快速）
filterwarnings =
    ignore::DeprecationWarning
